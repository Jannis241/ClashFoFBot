use crate::prelude::*;

pub fn create_data_augmentation(image_path: &Path, num_of_creations: i32) {
    let labels_path = Path::new("dataset/labels");
    let images_path = Path::new("dataset/images");
}
